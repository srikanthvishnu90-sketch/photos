{
  "name": "gems-ios",
  "version": "0.1.0",
  "private": true,
  "description": "Native iOS shell (Capacitor) for the Gems photos web app, with a custom PhotoKit plugin for whole-library analysis.",
  "scripts": {
    "sync:web": "node scripts/sync-web.mjs",
    "cap:sync": "npm run sync:web && npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
